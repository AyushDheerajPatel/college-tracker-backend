name: Keep Render Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *" # Har 10 minute
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Backend
        run: |
          curl -L https://college-tracker-backend.onrender.com/